import * as path from "path";
export const WALLET_FILE = path.join(process.cwd(), "wallets", 'solana_wallet.json');
export const IMPORT_WALLET_FILE = path.join(process.cwd(), "wallets", 'import_wallet.json');
;
